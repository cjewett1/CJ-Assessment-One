/*Module is reusable code. 
    Export the functionality of the module
    Default export
    var, function, obj, array, class...

*/

function getFetcher() {
    return "GET REQUEST"; //GET should only retrieve data. POST sends new data.
}pwd